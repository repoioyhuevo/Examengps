<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="16dp"
    android:paddingTop="16dp"
    android:paddingRight="16dp"
    android:background="@drawable/pagebkg"
    android:paddingBottom="16dp"
    tools:context=".Principal">

    <Button
        android:id="@+id/btnIrmapa"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:layout_marginBottom="16dp"
        android:text="Ir a Mapa" />

    <Button
        android:id="@+id/btnGoToMostrarDatos"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/btnIrmapa"
        android:layout_centerInParent="true"
        android:layout_marginTop="16dp"
        android:text="Ir a MostrarDatosActivity" />

    <ImageView
        android:id="@+id/imageView"
        android:layout_width="234dp"
        android:layout_height="359dp"
        android:layout_alignParentStart="true"
        android:layout_alignParentTop="true"
        android:layout_alignParentEnd="true"
        android:layout_alignParentBottom="true"
        android:layout_marginStart="115dp"
        android:layout_marginTop="32dp"
        android:layout_marginEnd="126dp"
        android:layout_marginBottom="372dp"
        app:srcCompat="@drawable/logo" />

    <Button
        android:id="@+id/btnGoToHistorial"
        android:layout_width="380dp"
        android:layout_height="wrap_content"
        android:layout_below="@id/btnGoToMostrarDatos"
        android:layout_marginTop="16dp"
        android:text="Ir al Historial" />
</RelativeLayout>
